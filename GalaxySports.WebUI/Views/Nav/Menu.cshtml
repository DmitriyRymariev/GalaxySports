@model IEnumerable<string>
@{ 
    Layout = null;
}
<ul>
    <li>
        @Html.ActionLink("All products", "List", "Product")
    </li>
    @if (Model != null)
    {
        foreach (var link in Model)
        {
        <li>
            @Html.RouteLink(link, new
    {
        controller = "Product",
        action = "List",
        category = link,
        page = 1
    },
    new
    {
        @class = link == ViewBag.SelectedCategory ? "selected" : null
    })
        </li>
        }
    }
</ul>
